# SpecAgent 评测方案

> 评测是这个项目的护城河。简历上的每一个数字都来自这里，且必须脚本可复现。
> 所有评测集放在 `evals/`，所有结果写入 README 数字面板。

## 评测一：检索分级评测（M1 核心产出）

- **目的**：证明混合检索+重排的价值，拿到三档对比数字
- **评测集构建**：从语料中抽 50 个条文，每条人工写 1 个"工程师会怎么问"的 query
  （同一条文可写同义改写变体），标注正确条款号作为 ground truth
- **方法**：同一评测集跑三档配置
  1. 纯向量检索（pgvector）
  2. +BM25 混合（RRF 融合）
  3. +bge-reranker 重排
- **指标**：recall@5（正确条文是否在 top5）、MRR、平均延迟
- **产出**：三档对比表；BadCase 归因记录（如表格类条文被切碎 → 改结构化分块）

## 评测二：审查准确率——人工埋错法（M2 核心产出）

- **目的**：测审查的漏报/误报，这是全项目最有说服力的数字
- **评测集构建**：自制 30 份技术文档（每份 10~20 个应查项），**故意埋入已知不符合项**
  （每份埋 3~6 处，类型覆盖：尺寸超差、缺失必填项、引用过期标准等），
  同时保证一部分文档完全合规。埋错位置和类型记录在标注文件里
- **方法**：全量跑审查流水线，比对发现项与埋错标注
- **指标**：漏报率（埋了没查出）、误报率（没埋报了不符合）、结论与证据一致性
- **产出**：漏报率 / 误报率 + BadCase 归因（解析问题 / 检索问题 / 推理问题 / 门控问题）

## 评测三：拒答正确率

- **目的**：证明"不硬答"是设计出来的而不是碰运气
- **评测集构建**：
  - 应拒样本：超出语料范围的问题（A 级）、证据不足场景（B 级）、模糊扫描件（C 级）各 10 条
  - 不应拒样本：正常可判定问题 30 条
- **指标**：应拒样本的正确拒答率（且拒对等级）、不应拒样本的误拒率
- **产出**：分级拒答的 precision / recall

## 评测四：长期记忆消融

- **方法**：同一批审查用例跑两组——有经验库召回 vs 屏蔽记忆，其余配置完全一致
- **指标**：漏报率、审查耗时、重复 BadCase 复现次数
- **产出**：两组差值。**允许负结果**：如果记忆无显著提升，如实记录并归因
  （如：用例间场景差异太大，经验泛化不动）——负结果 + 归因同样是面试素材

## 评测五：上下文压缩对比

- **方法**：长审查任务（≥20 检查项）跑三种上下文策略：
  1. 全量历史
  2. 滑动窗口
  3. 滚动窗口 + Compact 摘要压缩
- **指标**：token 消耗、审查结论一致性（与全量历史的结果对比）、漏报率变化
- **产出**：token 节省百分比 + 质量变化

## 评测六：断点恢复测试

- **方法**：审查进行中 `kill -9` 后端进程（脚本在固定 step 注入），重启后续跑至完成，
  校验：无重复结论、无丢失检查项、findings 与 uninterrupted 基线一致
- **指标**：恢复成功率（10 次注入）、结果一致性
- **产出**：断点续跑成功率数字

## 评测七：回归评测

- **目的**：每次改 Prompt / 换模型 / 调检索参数后防止效果回退
- **方法**：固定评测一 + 评测二 + 评测三的子集作为回归集，改动前后各跑一遍
- **产出**：版本间对比表（进 git，与 commit 关联）

## 数字面板模板（完成后填入 README）

```
检索 recall@5:  纯向量 0.__ → 混合 0.__ → +rerank 0.__   (evals/retrieval)
审查漏报率 __% / 误报率 __%  (30 份埋错文档)              (evals/review)
拒答: 应拒正确拒 __%, 不应拒误拒 __%                      (evals/refusal)
记忆消融: 漏报率 __% → __%                               (evals/memory)
压缩: token 省 __%, 结论一致率 __%                        (evals/compression)
断点续跑成功率 __% (n=10)                                (evals/checkpoint)
pytest: __ passed
```

## 评测纪律

- 评测集先于调参存在：先建集，再改系统，禁止"看着结果挑测试集"
- 每组数字注明评测日期、代码 commit、配置版本
- 负结果照常记录——带归因的负结果在面试里比好看的数字更有说服力
