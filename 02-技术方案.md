# SpecAgent 技术方案

## 1. 六层架构

```
┌─────────────────────────────────────────────────┐
│ L0 接入层   FastAPI REST + SSE 流式 / Vue3 前端   │
├─────────────────────────────────────────────────┤
│ L1 编排层   LangGraph 状态机（supervisor 模式）   │
│   ①路由Agent → ②检索Agent → ③审查Agent           │
│        → ④校验Agent → ⑤报告Agent                 │
├─────────────────────────────────────────────────┤
│ L2 RAG 层   多模态解析 → 结构化分块 → pgvector    │
│             + BM25 混合检索 → rerank → 引用溯源   │
├─────────────────────────────────────────────────┤
│ L3 记忆层   工作记忆（滚动窗口+Compact摘要压缩）   │
│             长期记忆（历史审查结论经验库）          │
├─────────────────────────────────────────────────┤
│ L4 可靠性   幂等写入口 / checkpoint断点续跑        │
│             超时重试+熔断 / 执行轨迹审计 / 终止条件 │
├─────────────────────────────────────────────────┤
│ L5 安全层   证据门控（无证据不得下结论）            │
│             工具白名单沙箱 / 危险操作人工确认       │
└─────────────────────────────────────────────────┘
  L6 评测层（横切）：检索评测 / 埋错评测 / 消融 / 回归 / BadCase 归因
```

## 2. L1 五个 Agent 分工（2026-09-20 起为级联判定架构）

| Agent | 职责 | 对应技术点 |
|-------|------|-----------|
| ① 路由 | 判断任务类型与专业领域。关键词/向量**快速路由**优先，低置信再 Query Rewrite + LLM 意图识别；支持多意图拆分 | 意图识别与智能路由 |
| ② 检索 | 批量混合检索（一次 embedding + 单连接），产 RRF 序候选池；**rerank 属于慢路**，快路比较器不需要精排 | RAG / 混合检索 / rerank |
| ③ 审查（planner） | **级联判定**：快路 = 规则比较器直判（双门禁：已知属性 + 领域守卫）；判不动进慢路（精排 → 拒答阈值 → LLM，8 路并发） | Plan-and-Execute / 级联路由 |
| ④ 校验（checker） | 独立核对证据链：条文存在？引用属实？证据支持结论？ | 证据门控 / LLM 输出安全门控 |
| ⑤ 报告（writer） | 结构化审查报告（Pydantic 约束），结论挂三元组证据 | 结构化输出 |

**级联判定（延迟优化的架构答案，P25）**：全项问 LLM = 法院所有案子都走合议庭。
快路比较器 ~1ms 判走覆盖域内 ~85-90% 的项，慢路只接判不动的项——100 项文档
16s（模板覆盖内 0 次 LLM 调用），对照全项慢路 6 项就要 124s。拒答语义随之更
干净：C 级在检索前（router），A/B 只在慢路判（判不动才需要问"没覆盖还是
依据不足"）。慢路三阶段（串行抓工作记忆快照 → 并发调 LLM → 串行收尾）保证
并发只覆盖网络往返、共享状态读序确定。

**降级路径**：跑不动五 agent 时合并为三 agent（路由 / 检索+审查 / 校验+报告），
架构叙述不变，简历表述不缩水。

## 3. L2 RAG 层设计

- **语料**：公开标准目录/摘要元数据 + 自写模拟标准文档（版权安全，README 注明 demo 语料）
- **解析**：PyMuPDF 提文本与版面结构；图表用 Qwen-VL 生成语义描述，**与文本一起入索引**
- **分块**：按「章→节→条」层级结构化切分，chunk 元数据带条款号、版本号（条款号即天然主键，
  也是引用溯源的锚点）
- **索引**：pgvector（HNSW 索引）+ PostgreSQL 全文检索（BM25 系）同库双通路
- **检索**：双通路召回 → RRF 融合 → bge-reranker-v2-m3 重排 → top-k
- **多级拒答判定**：
  - A 级：top-k 分数均低于阈值且无相关条文 → "标准库未覆盖该项"
  - B 级：有相关条文但校验 Agent 判定证据不支持结论 → "相关但依据不足"
  - C 级：输入文档解析置信度低（如扫描件模糊）→ "输入解析存疑，请人工复核"

## 4. L4 可靠性设计（继承自"虚拟人决策引擎"的已验证方案）

- **幂等**：所有状态变更经唯一写入口；审查轨迹表以 (review_id, step_seq) 唯一键约束，
  重复提交同一步直接跳过
- **断点续跑**：LangGraph checkpointer（Postgres 存 checkpoint），重启后从最后完成的
  node 继续；kill -9 测试脚本化
- **重试+熔断**：LLM 调用指数退避重试 3 次；滑动窗口内连续 N 次失败触发熔断，
  该检查项标记"服务降级"跳过，主流程不中断
- **终止条件**：成功 / 步数上限 / 预算上限（token 花费）/ 检查项耗尽 / 死锁 / 异常
- **轨迹审计**：每步记录输入摘要、输出摘要、耗时、token 消耗，报告页可回放

## 5. L5 安全设计

- **证据门控**：门控逻辑独立于审查 Agent 的 LLM 调用，代码级强制执行
  （先程序化校验条款号存在 + 引用文本确实为 chunk 子串，再交 LLM 判定支持性）
- **工具白名单**：agent 可调用的工具集合显式声明；任何写文件/执行命令类操作
  默认拒绝，需要时人工确认

## 6. 技术选型表（含面试防御理由）

| 类别 | 选型 | 为什么（面试被问就这么答） |
|------|------|--------------------------|
| 语言/框架 | Python 3.11 + FastAPI | 异步生态成熟；SSE 流式输出审查过程 |
| 编排 | LangGraph | 显式状态机，checkpointer 原生支持断点续跑；比 Chain 的隐式链路可审计 |
| 向量库 | **pgvector**（不用 Chroma/FAISS） | Chroma/FAISS 被面试官视为"demo 级"：没处理过分片、ANN 索引调优、持久化运维；pgvector 一套 PostgreSQL 同时解决向量+全文检索+业务表，少一个组件；HNSW 参数（m/ef_construction/ef_search）可讲调优 |
| Embedding | BGE-M3（本地） | 中英双语、支持长文本，本地跑无 API 成本 |
| Rerank | bge-reranker-v2-m3（本地） | 检索三级对比评测的第三档 |
| 主模型 | DeepSeek API | 便宜、结构化输出稳定；换模型只改配置（回归评测会对比） |
| 视觉 | Qwen-VL | 图表语义描述；延续实习技术栈 |
| 缓存/限流 | Redis | LLM 响应缓存、接口限流、熔断计数器 |
| 前端 | Vue3 + TypeScript | 复用已有技术栈，只做最小界面 |
| MCP | Python 官方 SDK | 三个工具：standard_search / document_parse / citation_lookup |

## 7. 核心数据模型（PostgreSQL）

```
standards        标准文档：id, 标准号, 名称, 版本, 来源
chunks           条文分块：id, standard_id, 条款号, 章节路径, 文本, 图表描述,
                 embedding vector(1024), tsv 全文索引
documents        待审文档：id, 文件名, 类型, 解析状态
check_items      检查项：id, document_id, 描述, 解析置信度
reviews          审查单：id, document_id, 状态, 预算, 起止时间
review_steps     轨迹：id, review_id, step_seq(唯一键), agent, 输入摘要,
                 输出摘要, 耗时, token, 状态
findings         结论：id, review_id, check_item_id, 结论(符合/不符合/拒答等级),
                 置信度, chunk_id(证据), 引用原文, 校验状态
memory_entries   长期记忆：id, 文档特征, 不符合项模式, 引用次数
```

## 8. 目录结构

```
spec-agent/
├── backend/
│   ├── api/            # FastAPI 路由 + SSE（AI 可代写）
│   ├── agents/         # langgraph：router/retriever/reviewer/verifier/writer【亲手写】
│   ├── rag/            # parse/chunk/index/retrieve/rerank【检索融合与调用逻辑亲手写】
│   ├── memory/         # working(压缩) / long_term(经验库)【亲手写】
│   ├── reliability/    # checkpoint / circuit_breaker / idempotent_store【亲手写】
│   └── safety/         # evidence_gate / tool_whitelist【亲手写】
├── mcp_server/         # MCP 封装
├── evals/              # 评测集、评测脚本、结果报告【亲手写】
├── web/                # Vue3 前端（AI 可代写）
├── docker/  k8s/
└── docs/               # 模块设计说明、防守笔记、BadCase 归因
```

## 9. 技术清单覆盖映射（简历口径核对表）

| 技术点 | 项目落点 |
|--------|---------|
| Python + FastAPI | L0 |
| Function Calling / Tool Use | 全部工具调用 + 白名单 |
| Prompt Engineering | 各 agent 结构化约束、few-shot、调优记录进 docs/ |
| RAG + 向量库 | L2 全套（pgvector/混合/rerank/拒答/多模态） |
| Agent 框架 + ReAct/Plan-Execute | LangGraph + 审查 planner |
| 大模型 API + 结构化输出 | DeepSeek/Qwen-VL + Pydantic |
| 工程基础 | PG/Redis/Docker/pytest/日志 |
| Context Engineering | L3 工作记忆压缩 + 对比实验 |
| Memory | L3 长期经验库 + 消融 |
| Multi-Agent | L1 五 agent supervisor |
| MCP | mcp_server |
| 评测体系 | L6 七套评测 |
| 可靠性（幂等/熔断/断点续跑） | L4 |
| AI Coding | Claude Code 参与开发 + MCP 接入演示 |
| 意图路由 | 路由 Agent |
| 安全沙箱 | L5 |
