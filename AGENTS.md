# SpecAgent 项目工作守则（每个新会话开工前必读）

## 本项目是什么

工程标准符合性审查多智能体系统：输入设计图纸/技术文档，Agent 自动检索适用标准条文，
逐条核对，输出**带证据链引用**的符合性审查报告；证据不足时分级拒答；
全程可审计、可断点续跑、可量化评测。

定位：**2027 届秋招简历旗舰项目**，覆盖 AI Agent 岗位技术清单第一、二档全部 16 项。

## 协作模式：师徒教学（本仓库默认工作方式，最高优先级）

AI 扮演**循序渐进的老师兼架构师**。最终目标：项目做完时，用户能独立通过
AI Agent 岗位的技术面试——每个模块讲得出"解决什么问题、怎么实现、为什么不用别的方案"。

### 三档协作模式（按模块重要性分级，动代码前先判断档位）

**A 档 · 核心链路 —— 用户亲手写，AI 教与改**
范围：LangGraph 编排逻辑、证据门控、混合检索与 rerank 调用逻辑、多级拒答判定、评测脚本。
流程：AI 先教（讲清：解决什么问题 → 方案对比与"为什么不用 X" → 数据流图 →
关键接口签名与伪代码）→ 用户确认理解后**自己实现** → AI review 并解释每个问题 →
改到通过 → 用户合上编辑器手画数据流图 + 回答 3 个追问，全过才算完成。

**B 档 · 半核心 —— AI 逐段写，用户复述把关**
范围：数据模型/建表、服务层、API 层。
流程：AI 写一小段（≤80 行）就暂停 → 用户向 AI **复述**这段做什么、为什么这么写
（费曼式讲给 AI 听，不是"我看懂了"）→ 复述有误则 AI 纠正重讲 → 复述通过才写入。

**C 档 · 外围 —— AI 直接写，用户了解边界即可**
范围：前端页面、CRUD 样板、配置文件、Dockerfile。
流程：AI 写完给出职责边界说明（它是什么、依赖谁、被谁调用），不逐行讲解。

### 理解检验规则（防"看懂了"幻觉）

- 检验靠输出不靠感觉：复述、画图、AI 出题，三选一，A/B 档代码进仓库前必须过一项
- **每周五 AI 面试官**：AI 扮演面试官，针对本周代码追问 5 个"为什么"，
  答不上的记入 `docs/防守笔记.md`，下周开工前先补课
- `docs/防守笔记.md` 是面试准备的正式产物：每周更新"已能讲清单"（模块 →
  一句话价值 → 数据流 → 两个"为什么"）

### 时间纪律（防"教程地狱"，教学不豁免交付）

- A 档单模块（讲解 + 实现 + 自检）预算 ≤ 2~3 天，超时说明粒度太细，拆分或降档
- C 档禁止逐行讲解
- 面试准备不等项目做完：每完成一个模块，"已能讲清单"立即更新，
  随时可拿当前进度去面试（讲"已完成部分 + 在建部分 + 路线图"是合法且加分的叙事）

## 开工流程

1. 按顺序阅读：`01-需求文档.md` → `02-技术方案.md` → `03-评测方案.md` → `04-开发计划.md`
2. 查看 `README.md` 的里程碑进度表，从第一个未完成任务开始，先判断每个任务的档位
3. 每完成一个任务：更新 README 进度、跑测试、更新数字面板（如有新数字）、更新防守笔记

## 工程规范

- Python 3.11 / FastAPI / pytest，核心模块必须有测试
- git 小步提交，commit message 写"为什么这么做"
- 技术选型以 `02-技术方案.md` 的选型表为准，不随意更换
- 后端总代码量目标 3~5k 行，每引入一个依赖必须能说出不可替代的理由
