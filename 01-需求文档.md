# SpecAgent 需求文档（PRD）

## 1. 背景与问题

工程与制造领域，设计图纸和技术文档必须符合国家标准/行业标准/企业规范。
现状是人工审查：标准文档厚、条文分散，审查慢、易漏、高度依赖老师傅经验。

用 LLM 直接做"文档问答"解决不了这个问题，因为审查需要的是**带证据的结论**：
"依据 GB/T XXXX 第 X.X 条，图纸中 XX 项不符合"。无证据的结论等于幻觉，
在工程场景是不可接受的。这正是本项目的核心命题：**让 Agent 只在有证据时下结论**。

## 2. 目标与非目标

### 目标
- 输入一份图纸/技术文档，自动产出符合性审查报告，每个结论挂证据三元组
  （条文原文 + 出处条款号 + 置信度）
- 证据不足时分级拒答，绝不硬答
- 全程执行轨迹落库可审计，支持断点续跑
- 七组评测数字支撑效果声明

### 非目标（明确不做，防止范围膨胀）
- 不做标准的全文收录与版权处理（用公开元数据 + 自写模拟标准语料）
- 不做多用户/权限系统（单用户工具即可）
- 不做高并发生产部署（能 Docker 起来演示即可）
- 不追求审查 100% 准确（追求的是漏报/误报/拒答可量化、证据可溯源）

## 3. 核心用户流程

```
上传图纸/PDF → 路由Agent识别领域与任务类型
  → 解析出检查项清单
  → 检索Agent为每个检查项召回候选条文（混合检索+重排）
  → 审查Agent逐项核对，产出初步结论
  → 校验Agent独立核对证据链（条文存在？引用属实？证据支持结论？）
      ├─ 证据充分 → 结论写入报告（挂三元组）
      └─ 证据不足 → 分级拒答（A标准未覆盖 / B相关但依据不足 / C输入解析存疑）
  → 报告Agent生成结构化审查报告 → 前端展示，点击结论可溯源到条文与图纸位置
```

## 4. 功能需求

### P0（MVP，M1+M2 交付）

| # | 功能 | 说明 |
|---|------|------|
| 1 | 标准语料入库 | PDF 解析（文本+版面）、图表用 VLM 生成语义描述、按「章→节→条」结构化分块、条款号作为元数据主键 |
| 2 | 混合检索服务 | pgvector 向量检索 + PostgreSQL 全文检索双通路 → bge-reranker 重排，返回条款号+原文+分数 |
| 3 | 检索分级评测 | 50 条 QA 评测集，跑出纯向量 / +BM25 / +rerank 三档 recall@5 与 MRR |
| 4 | Agent 审查流水线 | LangGraph 编排：路由 → 检索 → 审查 → 校验 → 报告（可先实现三 agent 降级版：路由/检索审查合并/校验报告合并） |
| 5 | 意图路由 | 关键词/向量快速路由优先，低置信度再走 Query Rewrite + LLM 意图识别；支持多意图拆分 |
| 6 | 多级拒答 | A 标准库未覆盖 / B 相关但依据不足 / C 输入解析存疑，三种输出话术 |
| 7 | 证据门控 | 校验 Agent 独立于审查 Agent：条文号必须真实存在、引用文本必须确实出自该 chunk、证据必须支持结论，否则强制降级为拒答 |
| 8 | 结构化报告 | Pydantic 约束的报告 Schema；每个结论挂三元组证据；SSE 流式输出过程 |
| 9 | 执行轨迹审计 | 每步经唯一写入口落库，审查轨迹表唯一键保证整单幂等 |

### P1（M3 交付）

| # | 功能 | 说明 |
|---|------|------|
| 10 | 断点续跑 | LangGraph Postgres checkpointer；进程 kill -9 后重启可从中断步骤恢复；自动化测试演示 |
| 11 | 熔断降级 | LLM/重排服务失败：指数退避重试 3 次 → 连续失败熔断，该检查项标记"服务降级"，主流程继续 |
| 12 | 长期记忆 | 历史审查结论沉淀为经验库（文档特征→常见不符合项），新审查时检索召回；做有/无记忆消融 |
| 13 | 上下文压缩 | 长审查任务的工作记忆：滚动窗口 + Compact 摘要压缩；做压缩前后 token/成功率对比 |
| 14 | MCP Server | 把标准检索、文档解析、条文引用封装成 MCP 工具，接入 Claude Code 演示 |
| 15 | 前端最小界面 | Vue3：上传、实时流转可视化、报告展示与证据溯源点击 |

### P2（锦上添花，时间富余才做）

| # | 功能 | 说明 |
|---|------|------|
| 16 | 审查准确率评测完善 | 人工埋错法扩展到更多文档类型 |
| 17 | Ollama 离线模式 | 本地小模型跑通全流程，无外网可演示 |
| 18 | K8s 部署清单 | 一份 Deployment/Service yaml + 扩展性说明 |

## 5. 成功标准

1. 端到端 demo：上传 → 实时看 agent 流转 → 输出带证据链报告，全程 ≤ 3 分钟
2. 数字面板 7 组数字全部有值且脚本可复现
3. 后端 3~5k 行，pytest 全绿
4. 作者（人类）能合上编辑器画六层架构图，并对每个选型回答"为什么不用 X"
5. 能通过模拟面试：任意抽一个模块追问 3 层"为什么"都能答
