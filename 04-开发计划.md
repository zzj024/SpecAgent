# SpecAgent 开发计划

> 时间盒：6 周（可与 StoryGuard 项目并行，注意每天聚焦一个项目）
> 原则：任意阶段停下都是完整可讲的项目，不存在烂尾状态

## M1 — RAG 底座（第 1~2 周）

### 任务
1. 语料准备：公开标准元数据 + 自写 3~5 份模拟标准文档（含图表）
2. 解析入库：PyMuPDF 文本/版面 + Qwen-VL 图表语义描述
3. 结构化分块：章→节→条层级切分，条款号元数据
4. pgvector 建表 + HNSW 索引；PostgreSQL 全文检索字段
5. 混合检索（RRF 融合）+ bge-reranker 重排服务
6. **评测一**：50 条 QA 评测集 + 三档对比跑分
7. FastAPI 检索接口

### 验收标准
- [ ] `python -m evals.retrieval` 一键重跑三档数字
- [ ] recall@5 三档数字进 README
- [ ] 检索模块数据流图能手画，HNSW 参数能说出含义
- [ ] pytest 覆盖分块与融合逻辑

## M2 — Agent 编排（第 3~4 周）

### 任务
1. LangGraph 状态机：state 定义、五 node（可先三 node 降级版）、条件路由
2. 路由 Agent：快速路由 + Query Rewrite 兜底 + 多意图拆分
3. 审查 Agent：Plan-and-Execute（先生成核对计划再逐项执行）
4. 校验 Agent：程序化校验（条款存在 + 引用子串）+ LLM 支持性判定
5. 多级拒答（A/B/C）接入流水线
6. 结构化报告 + SSE 流式过程输出
7. 轨迹落库（唯一写入口 + 唯一键幂等）
8. **评测二 + 三**：埋错法 30 份文档、拒答评测集
9. 前端最小页面（上传 / 流转可视化 / 报告溯源）

### 验收标准
- [ ] 端到端 demo ≤ 3 分钟：上传 → 流转 → 带证据链报告
- [ ] 漏报率/误报率、拒答正确率进 README
- [ ] LangGraph state 流转图能手画；能讲 supervisor 模式与降级路径
- [ ] 抽查任一 agent 能答"为什么需要它，合并会怎样"

## M3 — 生产化与评测补全（第 5~6 周）

### 任务
1. LangGraph Postgres checkpointer + kill -9 恢复测试脚本（**评测六**）
2. 重试 + 熔断 + 降级标记（Redis 计数器）
3. 长期记忆经验库 + **评测四**（记忆消融）
4. 工作记忆压缩（滚动窗口 + Compact 摘要）+ **评测五**
5. MCP server（standard_search / document_parse / citation_lookup）+ Claude Code 接入演示
6. **评测七**回归集固定 + 换模型对比（DeepSeek vs Qwen）
7. Docker Compose 一键启动；README 数字面板填完
8. docs/：BadCase 归因文档 + 面试防守笔记（每个模块"为什么不用 X"）

### 验收标准
- [ ] 数字面板 7 组全有值且脚本可复现
- [ ] kill -9 注入 10 次恢复成功率有数字
- [ ] Claude Code 里能真实调用 MCP 工具演示
- [ ] 模拟面试：任意模块追问 3 层"为什么"能答
- [ ] 后端 3~5k 行、pytest 全绿

## 风险与对策

| 风险 | 对策 |
|------|------|
| 五 agent 太重跑不动 | 启用三 agent 降级路径，架构叙述不变 |
| 语料版权 | 只用公开元数据 + 自写模拟标准，README 注明 |
| 时间不够 | 优先保 M1 检索数字与 M2 端到端 demo（简历两大卖点）；M3 的记忆/压缩评测可压缩为小规模 |
| 与 StoryGuard 并行冲突 | 单双日分项目或按周切换；两项目共用 pgvector/LangGraph 经验，切换成本递减 |
| AI 代写失控 | 遵守 AGENTS.md：核心链路亲手写，AI 只解释与 review |
